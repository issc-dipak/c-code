#include <stdio.h>
void caalculat(float value);
int main()
{
    float value = 100.0;
    calculat(value);
    return 0;
}
void caalculat(float value)
{
    value = value + (0.18 * value);
    printf * ("final price is :%f");
}