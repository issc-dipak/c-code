#include <stdio.h>

int main() {
    int n, i, sum = 0;

    printf("Enter the  elements: ");
    scanf("%d", &n);

    int arr[n];

    printf("E %d \n", n);
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    for(i = 0; i < n; i++) {
        
    }

    printf("Sum of elements = %d\n", sum);

    return 0;
}
